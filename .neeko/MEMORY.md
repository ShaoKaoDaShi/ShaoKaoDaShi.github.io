# 项目记忆

## PITFALL

- [2026-05-22 09:40] [PITFALL] [DEBUGGING] WHEN 需要用 lint/测试验证本次改动时 — PROBLEM 直接运行仓库级命令容易被无关子项目或工作树问题阻塞，导致验证信号不可靠 — FIX 先识别 monorepo/嵌套 worktree 边界并优先执行针对改动文件或当前包的定向校验，必要时再说明全量校验受外部噪音影响 [mid:1c9f973f]
- [2026-05-25 17:44] [PITFALL] [DEBUGGING] WHEN: 读取或校验可能带 UTF-8 BOM 的 CSV 表头并用列名定位字段时 PROBLEM: 第一列表头会包含隐藏 BOM 导致列名匹配失败或校验误报 FIX: 解析表头后先去除首个字段的 BOM 再建立列名索引和执行校验 [mid:30a6a423]
- [2026-05-25 17:44] [PITFALL] [IMPLEMENTATION] WHEN 需要批量改写用户数据文件/CSV — PROBLEM 直接原地修改且无备份/临时输出会在列错位后难以恢复，尤其未纳入 git 的文件 — FIX 先创建 .bak 或写入临时文件，完成结构校验和抽样校验后再替换原文件 [mid:c4936913]
- [2026-05-25 17:44] [PITFALL] [REPEATED_ERROR] WHEN 为 CSV 增删列或补列 — PROBLEM 在已被污染的文件上连续打补丁会叠加错位并让验证信号失真 — FIX 以干净输入为唯一数据源，按表头定位列，断言每行列数一致，写后重新解析校验目标列和关键原始列 [mid:d2f5155d]

## STRATEGY

- [2026-05-22 09:40] [STRATEGY] [IMPLEMENTATION] 在执行“提交代码”这类模糊指令时，先用 git status + 定向 diff 明确改动边界，再按主题分批暂存提交并显式排除工具产物/无关文件 → 能避免把环境文件或临时目录误提交，且可连续处理多次提交请求而不混淆范围 [mid:1ff8b0df]
- [2026-05-25 17:44] [STRATEGY] [USER_CORRECTION] 将一次性数据转换沉淀为幂等脚本（存在列则更新，不存在列则追加，支持指定输入文件）→ 可重复运行、便于验证，也能降低手工修补数据文件的风险 [mid:3f4e9925]
